# 遗留事项（2026-09-17 收工）

关机前状态留档。本文件纯记录，未对任何文件或进程做处理。

## 1. 无窗口 allegro 空转进程（关机即解决）

收工时仍有 3 个 `allegro.exe`，全部 `HasWindow=False`（不是交互式会话），
启动时间落在上一轮 `-nograph` 探测期间，判断是探测留下的孤儿进程：

| pid | 启动时间 | 内存 | 累计 CPU |
|---|---|---|---|
| 11640 | 21:18:38 | 2202 MB | 2832 s |
| 17736 | 21:13:46 | 2936 MB | 3115 s |
| 25676 | 21:16:09 | 1426 MB | 2964 s |

每个约 50 分钟 CPU / 约 50 分钟墙钟 = 100% 满载空转。它们持续往
`D:\Aphranda\APTools\Temp\allegro.jrl` 追加，实测增速 **2.56 MB/s（≈211 GB/24h）**，
收工时该文件已 **6.35 GB**。

**关机后自然终止，无需处理。** 下次若再出现同类情况：
先 `Get-Process allegro | Select Id,StartTime,MainWindowHandle`，
`MainWindowHandle -ne 0` 的**绝对不能动**（那是用户在用的会话），
全为 0 的才可能是孤儿。

> 教训：探测脚本用 `-WorkingDirectory` 指到了正式运行目录
> `D:\Aphranda\APTools\Temp`，导致 Allegro 的命令日志往这里堆。
> 以后探测应该用独立 scratch 目录（如 `Temp\_probe\`），别污染运行目录。

## 2. 收工时的磁盘占用（未处理，用户自行决定）

- `D:\Aphranda\APTools\Temp\allegro.jrl` —— 6.35 GB，可重建，未删除
- `D:\Aphranda\APTools\Temp\_fsrc18\` —— 目录，未动
- `D:\Aphranda\APTools_prod\` —— 15 个文件，已被 `D:\Aphranda\APTools` 取代的旧运行位置

## 3. 代码侧待办（详见 `APTools\docs\待办-静态检查器.md`）

1. **SKILL 静态检查器**（给 SKILL 补一个"编译器"）
   - 第一阶段（零误报，先做）：括号平衡与顶层 form 完整性、单元素括号包单 form、
     `if`/`setq` 实参个数、超 `INT_MAX` 字面量、`substring` 索引 0
   - 第二阶段（需白名单）：未定义函数、实参个数、变量是否赋值过
2. 把它挂到 `devkit\deploy.ps1` 作为部署门禁（目前只做 GBK 编码体检）
3. `AllegroTools` 仓库 git 提交推送（`APTools/`、`Temp/`、`_archive/` 仍未跟踪）
4. 工具②「符号封装一致性」仍是占位
5. 移植 FanySkill V1.8 其余明文源码（Align 三件套、`Change_WIDTH`、`Place_Inside`、
   `check_refdes_direction`）—— 未开始

## 4. 本次会话的成果（已完成并验证）

**电源网络刷色** `APTools\tools\90_misc\netcolor\APT_NetColor.il`

- 先识别（关键词 + 电压记号两路信号；地按 `GND` 匹配，`VSS` 家族另列）
- 再随机上色，每网一个不同随机色；**GND 系只在灰色系里随机**（R=G=B，默认 90~210）
- 排除表走后缀匹配且只作用于"本可算电源"的名字，普通信号不会被误列
- 真板 `ctl-synctrig4f4-hasl-ascii.brd`（186 网络）只读端到端验证：
  识别 GND 系 2 / 电源 10 / 排除 0 / 其它 174；上色 12 → 清除 0 →
  释放保留槽后调色板精确还原 → 再刷照常。板文件未被改动，无 `.lck` 残留。
- 离体自检 `devkit\tests\selftest_netcolor.il`：100+ 用例，零 `*Error*`

**新增** `devkit\il_parens.py`（括号平衡 / 顶层 form 完整性检查，当场抓出
`APT_NetColor.il` 第 554 行多出的一个右括号）

**文档** `docs\SKILL方言坑.md` 补到第 18 条（`substring` 1 起算、无 bignum、
`mod` 是函数、`itoa` 不存在、多余括号、括号错位的连锁反应、`errset` 语义、
`-nograph` 下看菜单的口子、只读开图验证）
